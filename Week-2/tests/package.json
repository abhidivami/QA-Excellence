{
  "name": "qa-week2-tests",
  "version": "1.0.0",
  "description": "Unit and API tests for Week-2 QA learning — Invoice Portal + Petstore API",
  "scripts": {
    "test":      "jest --verbose --runInBand",
    "test:unit": "jest unit/unit_tests --verbose",
    "test:api":  "jest api/api_tests  --verbose --runInBand"
  },
  "dependencies": {
    "axios": "^1.6.7"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  },
  "jest": {
    "testTimeout": 30000,
    "testEnvironment": "node"
  }
}
